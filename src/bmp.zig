//const std = @import("std");
//const types = @import("types.zig");
//const GenericImage = types.GenericImage;
//const BmpImage = types.BmpImage;
//
//pub fn enc(allocator: std.mem.Allocator, image: GenericImage) BmpImage {
//    const output = std.ArrayList(Pixel).init(allocator);
//
//    // why does bmp go bottom-up????
//    var y = image.height - 1;
//    while (y >= 0): (y -= 1) {
//        
//    }
//}